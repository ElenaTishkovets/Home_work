package home_work_2.arrays;

/**
 * Интерфейс реализует методы:
 * 1. вывода всех элементов массива в консоль
 * 2. вывода каждого второго элемент массива в консоль
 * 3. вывода всех элементов массива в консоль в обратном порядке
 */
public interface IArraysOperation {
    void printAllElements(int[] array);
    void printEverySecondElement(int[] array);
    void printReverseOrder(int[] array);

}

